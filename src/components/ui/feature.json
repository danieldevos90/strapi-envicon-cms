{
  "collectionName": "components_ui_features",
  "info": {
    "displayName": "Feature",
    "description": "Feature block with icon, title and description"
  },
  "options": {},
  "attributes": {
    "icon": {
      "type": "enumeration",
      "enum": ["check", "settings", "volume2", "building", "shield", "clock", "star", "heart"],
      "required": false
    },
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text",
      "required": true
    }
  }
}

