{
  "collectionName": "components_ui_logos",
  "info": {
    "displayName": "Logo",
    "description": "Logo component with media upload and alt text"
  },
  "options": {},
  "attributes": {
    "media": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "alt": {
      "type": "string",
      "required": false,
      "description": "Alt text for accessibility (falls back to media alternativeText if empty)"
    }
  }
}
